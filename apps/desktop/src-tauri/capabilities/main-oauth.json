{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-oauth",
  "description": "Spotify OAuth login capabilities, host window only",
  "windows": ["main"],
  "permissions": ["shell:allow-open", "oauth:allow-start", "oauth:allow-cancel"]
}
